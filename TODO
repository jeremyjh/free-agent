Managed Agent:
 * refactor common code in agent handlers

Executive:
 * handle commands: 
	* query execute action history
	* query event history 

* integrate scheduler

Plugins:
	* create NagiosAlerts
