Executive:
 * see if we can move logging entirely to agentCallHandler
 * handle commands: 
	* query execute action history
	* query event history 

* integrate scheduler

Plugins:
	* create NagiosAlerts
